name: vennro

services:
  redisinsight:
    image: redis/redisinsight:latest
    container_name: redisinsight-container
    restart: unless-stopped
    ports:
      - "5540:5540"
    networks:
      - proxy

networks:
  proxy:
    external: true
